/**
 * generated by template generator. don't modify it.
 *
 */

import GaiaRequest from '17zy_egg-gaiajs/app/lib/request';

/**
 *
 * {{description}}
 */
export default class {{name}}Request extends GaiaRequest {
  {% for field in parameters %}
  // {{field.description}}
  {{field.name}}{% !required %}?{% endif %}: {{field.schema.type}};
  {% endfor %}

  constructor(params: any) {
    super(params);

    {% for field in parameters %}
    this.{{field.name}} = {% if field.schema.type == "number" -%}
      parseInt(this.ctx.params.{{field.name}})
      {%- elif field.schema.type == "" -%}
      {%- else -%}
      this.ctx.params.{{ field.name }}
      {%- endif %};
    {% endfor %}
  }
}
