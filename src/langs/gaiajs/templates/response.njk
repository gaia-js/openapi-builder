/**
 * generated by template generator. don't modify it.
 *
 */

import GaiaResponse from '17zy_egg-gaiajs/app/lib/response';

import { Context } from 'egg';

$a-{for: name in getCustomNames(fields)}
import $a-{name} from '../type/$a-{name}';
$a-{end for}

/**
 * $a-{comment}
 */
export default class $a-{name}Response extends GaiaResponse {
	$a-{for: field in fields}
  /* $a-{field.comment} */
  $a-{field.name}: $a-{field.type.to};
	$a-{end for}

  output(ctx: Context) {
    return ctx.body = {
$a-{for: field in fields}
      $a-{field.name}: this.$a-{field.name},
$a-{end for}
    };
  }
}
